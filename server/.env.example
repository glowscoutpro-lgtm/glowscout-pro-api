PORT=4000
GOOGLE_MAPS_API_KEY=
ALLOWED_ORIGIN=*
# Beta feedback storage
FEEDBACK_DIR=./data
# Admin token required to read feedback via GET /api/feedback (omit to disable read endpoint)
ADMIN_TOKEN=
# Salt for hashing client IPs stored alongside feedback (any random string)
FEEDBACK_IP_SALT=
