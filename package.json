{
  "name": "glowscout",
  "version": "0.1.0",
  "private": true,
  "description": "GlowScout mobile app and API for finding highly reviewed nail and beauty professionals.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix app install",
    "dev:server": "npm --prefix server run dev",
    "dev:app": "npm --prefix app start",
    "typecheck": "npm --prefix server run typecheck && npm --prefix app run typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
